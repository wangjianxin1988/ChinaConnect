---
// /auth/register - alias for /auth/login (with mode=register via hash).
// The unified LoginPage component renders both modes; keeping a separate
// URL just for compatibility with older links.
return Astro.redirect("/auth/login#register");
---