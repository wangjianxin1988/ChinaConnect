---
// /auth/register - alias for the login page (with mode=register via hash).
import { authLangPrefix } from '@/components/user/auth-strings';
const lang = (Astro.locals.lang as string) || "en";
return Astro.redirect(authLangPrefix(lang) + "/auth/login#register");
---
