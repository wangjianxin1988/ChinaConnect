---
// /auth - unified entry: redirect to the login page in the detected language.
import { authLangPrefix } from '@/components/user/auth-strings';
const lang = (Astro.locals.lang as string) || "en";
return Astro.redirect(authLangPrefix(lang) + "/auth/login");
---
